# Scrubbed registry snapshots

This directory is reserved for reviewed Home Assistant registry snapshots that conform to `schemas/registry-snapshot.schema.json`.

Do not place raw Home Assistant `.storage` files here. Generated snapshots intentionally contain only the limited entity facts required for semantic binding and drift review.
