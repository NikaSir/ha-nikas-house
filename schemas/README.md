# Schemas

Machine-readable schemas for contracts, semantic inventory, panel manifests and generated-artifact metadata.

Schemas are introduced only when the corresponding real contract format is agreed. Validation must reject missing required semantics rather than silently applying unsafe defaults.
